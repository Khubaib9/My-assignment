
# Clone the repository to your local machine (replace "username" and "repository" with your GitHub username and repository name)
git clone https://github.com/username/repository.git

# Change into the local repository directory
cd repository C:\Program Files\R\R-4.3.2

# Copy your R script files into the repository directory

# Stage the changes
git add .

# Commit the changes
git commit -m "Add R script files"

# Push the changes to GitHub
git push origin main  # or use the branch name you created (e.g., master)
